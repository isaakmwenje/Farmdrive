import 'package:flutter/material.dart';

class AnalysisCaoresel extends StatefulWidget {
  const AnalysisCaoresel({Key? key}) : super(key: key);

  @override
  _AnalysisCaoreselState createState() => _AnalysisCaoreselState();
}

class _AnalysisCaoreselState extends State<AnalysisCaoresel> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child:Center(
        child: const Text(
          'ANALYSIS appears here',
        ),
      )
    );
  }
}
