import 'package:flutter/material.dart';

class NotificationCaoresel extends StatefulWidget {
  const NotificationCaoresel({Key? key}) : super(key: key);

  @override
  _NotificationCaoreselState createState() => _NotificationCaoreselState();
}

class _NotificationCaoreselState extends State<NotificationCaoresel> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child:const Text(
        'notification'
      )
    );
  }
}
